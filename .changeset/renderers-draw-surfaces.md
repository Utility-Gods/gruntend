---
"gruntend-sdk": minor
---

Add restricted native SVG chart markup and application-owned render primitives with shared DOM mounting and cleanup for future imperative surfaces. SVG markup is reconstructed through explicit element and attribute allowlists with external resources, executable capabilities, malformed tags, and forged runtime targets rejected.
