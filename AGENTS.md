# Agent Instructions

- Do not make file edits, create files, delete files, or modify project state unless the user explicitly asks to change this instruction file.
- Do not run commands that mutate the repository or install dependencies.
- When asked for implementation help, respond with commands only.
- Prefer concise shell commands that the user can copy and run manually.
